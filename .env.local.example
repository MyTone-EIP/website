# NextAuth Configuration
# ⚠️ IMPORTANT: Ce fichier contient des secrets. NE PAS committer sur Git!
# Ajouter ".env.local" à .gitignore

# Secret pour signer les tokens JWT (générer avec: openssl rand -base64 32)
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production

# URL de votre application
# Développement: http://localhost:3000
# Production: https://votre-domaine.com
NEXTAUTH_URL=http://localhost:3000

# URL de l'API backend (optionnel si vous changez d'URL)
# NEXT_PUBLIC_API_URL=https://api-mytone.onrender.com

# Logs NextAuth (optionnel pour le débogage)
# DEBUG=next-auth:*
